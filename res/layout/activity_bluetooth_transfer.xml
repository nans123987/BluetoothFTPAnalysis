<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.bluetoothftpanalysis.BluetoothTransfer" >

    <Button
        android:id="@+id/btnSwitchOnBluetooth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="SwitchOnBluetoothAdapter"
        android:text="@string/btnSwitchOnBluetooth" >
    </Button>

    <Button
        android:id="@+id/btnSearchActiveDevices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="SearchPairedDevices"
        android:text="@string/btnSearchActiveDevices" >
    </Button>

    <ListView
        android:id="@+id/deviceList"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="0.40"
        android:clickable="true"
        android:divider="#000080"
        android:dividerHeight="3dip"
        android:fastScrollEnabled="true"
        android:paddingLeft="5dp"
        android:paddingTop="30dp"
        android:scrollbarStyle="insideInset" >
    </ListView>

</LinearLayout>